# ตั้งค่าให้ใช้บนแท็บเล็ต Android + เปิด/บันทึกไฟล์จาก OneDrive

ทำครั้งเดียวจบ หลังจากนี้ใช้ได้เลยทั้ง PC และแท็บเล็ต

> **บน PC ไม่ต้องทำอะไรก็ใช้ OneDrive ได้อยู่แล้ว** — แค่ย้ายไฟล์ `.xlsx` ไปไว้ใน
> `C:\Users\<ชื่อคุณ>\OneDrive\...` แล้วกด **📂 เปิดไฟล์** เลือกจากตรงนั้น
> โปรแกรมเขียนทับไฟล์เดิมได้ตามปกติ แล้ว OneDrive จะ sync ขึ้นคลาวด์ให้เอง
>
> คู่มือนี้จำเป็นเมื่อต้องการใช้ **บนแท็บเล็ต** เท่านั้น

---

## ภาพรวม — ต้องทำ 3 อย่าง

| # | ทำอะไร | ใช้เวลา |
|---|---|---|
| 1 | เอาไฟล์โปรแกรมขึ้น GitHub Pages (ต้องมี HTTPS ถึงจะล็อกอิน Microsoft ได้) | ~10 นาที |
| 2 | จดทะเบียนแอปที่ Azure เพื่อขอสิทธิ์เข้าถึง OneDrive | ~5 นาที |
| 3 | ใส่ Client ID ลงใน `config.js` แล้วอัปเดตอีกรอบ | ~2 นาที |

**ทุกอย่างฟรี ไม่ต้องผูกบัตรเครดิต**

---

## ขั้นที่ 1 — เอาโปรแกรมขึ้น GitHub Pages

### 1.1 สมัคร GitHub (ข้ามได้ถ้ามีแล้ว)

เข้า <https://github.com/signup>

### 1.2 สร้าง repository

1. กด **+** มุมขวาบน → **New repository**
2. ตั้งชื่อ เช่น `ds-entry`
3. เลือก **Public**
   *(GitHub Pages ของบัญชีฟรีต้องเป็น Public — ไฟล์ที่อัปคือ **โค้ดโปรแกรมล้วน ๆ**
   ไม่มีข้อมูลลูกค้า ไม่มีไฟล์ `.xlsx` ไม่มี `customers.xlsx` ปนไปด้วย ดูรายการไฟล์ที่อัปในขั้น 1.3)*
4. กด **Create repository**

### 1.3 อัปโหลดไฟล์

วิธีง่ายสุดคือใช้สคริปต์ที่เตรียมไว้ให้ — เปิด PowerShell ในโฟลเดอร์ `ds-entry` แล้วสั่ง

```bash
powershell -ExecutionPolicy Bypass -File deploy-github.ps1 -RepoUrl https://github.com/<ชื่อคุณ>/ds-entry.git
```

สคริปต์จะสร้าง git repo ให้ คัดลอกเฉพาะไฟล์โปรแกรมไปที่โฟลเดอร์ชั่วคราว แล้ว push ขึ้นไป
**ไฟล์ที่ไม่ถูกอัปขึ้นไปเด็ดขาด:** `customers.xlsx`, `slip_log.json`, ไฟล์ `.xlsx` ทุกไฟล์, `serve.ps1`, `เปิดโปรแกรม.cmd`

ถ้าอยากอัปเอง: ลากไฟล์เหล่านี้ไปวางในหน้า repo (ปุ่ม **Add file → Upload files**)

```
index.html   slip-check.html   auth-callback.html   config.js
manifest.webmanifest   sw.js
lib/   icons/   vendor/
```

> โฟลเดอร์ `vendor/` ราว 17 MB (ตัวอ่าน OCR) — หน้าเว็บ GitHub อัปทีละ 100 ไฟล์ได้
> ถ้าอัปผ่านหน้าเว็บแล้วติด ให้ใช้สคริปต์แทน

### 1.4 เปิด GitHub Pages

ในหน้า repo → **Settings** → **Pages** (เมนูซ้าย)
- **Source**: `Deploy from a branch`
- **Branch**: `main` / โฟลเดอร์ `/ (root)` → **Save**

รอสัก 1–2 นาที แล้วจะได้ URL หน้าตาแบบนี้ — **จดไว้**

```
https://<ชื่อคุณ>.github.io/ds-entry/
```

---

## ขั้นที่ 2 — จดทะเบียนแอปที่ Azure

### 2.1 เข้า Azure Portal

<https://portal.azure.com> → ล็อกอินด้วย **บัญชี Microsoft ส่วนตัวเดียวกับที่ใช้ OneDrive**

> ถ้าถามให้สร้าง subscription หรือใส่บัตร — **ข้ามได้** App registration ไม่ต้องใช้ subscription

### 2.2 สร้าง App registration

ค้นหา **App registrations** ในช่องค้นหาด้านบน → กด **+ New registration**

กรอกดังนี้

| ช่อง | ใส่อะไร |
|---|---|
| **Name** | `DS แลกเงินกีบ` (ตั้งชื่ออะไรก็ได้ เห็นแค่คุณ) |
| **Supported account types** | เลือก **Personal Microsoft accounts only** |
| **Redirect URI** — ชนิด | เลือก **Single-page application (SPA)** ⚠️ สำคัญมาก |
| **Redirect URI** — ค่า | `https://<ชื่อคุณ>.github.io/ds-entry/auth-callback.html` |

กด **Register**

> ⚠️ **ต้องเลือกชนิด SPA เท่านั้น** ถ้าเลือก Web หรือ Mobile จะล็อกอินไม่ผ่าน
> (เบราว์เซอร์จะถูก CORS บล็อกตอนแลก token)
>
> ⚠️ Redirect URI ต้อง**ตรงเป๊ะ** ทุกตัวอักษร รวมทั้ง `/` ปิดท้ายโฟลเดอร์
> ค่าที่ถูกต้องหาได้จากในโปรแกรมเอง: กด **☁ เปิดจาก OneDrive** แล้วจะมีปุ่ม **คัดลอก Redirect URI** ให้

### 2.3 เพิ่ม Redirect URI สำหรับทดสอบบน PC (ไม่บังคับ)

ถ้าอยากทดสอบโหมด OneDrive จากเครื่องตัวเองผ่าน `เปิดโปรแกรม.cmd` ด้วย
ไปที่ **Authentication** → ใต้หัวข้อ **Single-page application** กด **Add URI** แล้วใส่

```
http://localhost:8787/auth-callback.html
```

### 2.4 คัดลอก Client ID

กลับไปหน้า **Overview** ของแอป → คัดลอกค่า **Application (client) ID**
หน้าตาแบบนี้ `11111111-2222-3333-4444-555555555555`

---

## ขั้นที่ 3 — ใส่ Client ID

เปิดไฟล์ `config.js` แก้บรรทัด `clientId` ให้เป็นค่าที่คัดลอกมา

```js
window.DS_CONFIG = {
  clientId: '11111111-2222-3333-4444-555555555555',
  dataFolder: 'DS-Entry',
};
```

แล้วอัปโหลด `config.js` ขึ้น GitHub อีกครั้ง (รันสคริปต์ `deploy-github.ps1` ซ้ำได้เลย)

---

## เสร็จแล้ว — วิธีใช้บนแท็บเล็ต

1. เปิด **Chrome** บนแท็บเล็ต ไปที่ `https://<ชื่อคุณ>.github.io/ds-entry/`
2. เมนู **⋮** → **เพิ่มลงในหน้าจอหลัก** (หรือกดปุ่ม **⬇ ติดตั้งแอป** ในโปรแกรม)
   → ได้ไอคอนบนหน้าจอ เปิดแล้วเต็มจอเหมือนแอปจริง
3. กด **📥 เตรียมใช้ออฟไลน์** หนึ่งครั้งตอนต่อ Wi-Fi
   → โหลดตัวอ่านสลิป (~17 MB) เก็บไว้ในเครื่อง หลังจากนี้อ่านสลิปได้แม้ไม่มีเน็ต
4. กด **☁ เปิดจาก OneDrive** → ล็อกอิน → เลือกไฟล์ `.xlsx`
5. กรอกข้อมูลตามปกติ → กด **💾 บันทึกลงไฟล์** → เขียนกลับลงไฟล์เดิมบน OneDrive

---

## เรื่องที่ควรรู้

### ต้องต่อเน็ตตอนไหนบ้าง

| ทำอะไร | ต้องมีเน็ตไหม |
|---|---|
| เปิดตัวโปรแกรม | ❌ ไม่ต้อง (แคชไว้แล้ว) |
| กรอกฟอร์ม แก้ตาราง | ❌ ไม่ต้อง |
| อ่านสลิปด้วย OCR | ❌ ไม่ต้อง (หลังกด "เตรียมใช้ออฟไลน์" แล้ว) |
| **เปิดไฟล์จาก OneDrive** | ✅ ต้องมี |
| **บันทึกกลับ OneDrive** | ✅ ต้องมี |

ข้อจำกัดนี้มาจากตัว Android + OneDrive เอง — แอป OneDrive บน Android
ไม่เปิดโฟลเดอร์ให้เบราว์เซอร์เขียนไฟล์ได้เหมือนบน PC

### ต้องล็อกอินใหม่ทุกกี่วัน

ประมาณ **วันละครั้ง** — เป็นกฎของ Microsoft เอง (refresh token ของเว็บแอปมีอายุ 24 ชั่วโมง)
กดล็อกอินแล้วทำงานต่อได้เลย ข้อมูลที่ค้างอยู่ในหน้าจอไม่หาย

### ข้อมูลอยู่ที่ไหนบ้าง

| ข้อมูล | เก็บที่ |
|---|---|
| ไฟล์ธุรกรรม `.xlsx` | ที่เดิมบน OneDrive ของคุณ (โปรแกรมไม่ย้ายไฟล์) |
| `customers.xlsx` (รายชื่อ ID ↔ ชื่อ) | โฟลเดอร์ **`DS-Entry`** บน OneDrive — โปรแกรมสร้างให้เอง |
| `slip_log.json` (ประวัติสลิป) | โฟลเดอร์ **`DS-Entry`** บน OneDrive |
| รูปสลิปที่ลากเข้ามา | ไม่ถูกเก็บและไม่ถูกส่งออกไปไหน — OCR อ่านในเครื่องล้วน |
| token ล็อกอิน | localStorage ของเบราว์เซอร์เครื่องนั้น ๆ เท่านั้น |

**ผลพลอยได้:** `customers.xlsx` กับ `slip_log.json` อยู่บน OneDrive แล้ว
→ ใช้ร่วมกันระหว่าง PC กับแท็บเล็ตอัตโนมัติ ไม่ต้องก๊อปไปมา
(บน PC ถ้าอยากให้ใช้ชุดเดียวกัน ให้กด **☁ เปิดจาก OneDrive** บน PC ด้วย
ถ้าเปิดผ่าน `เปิดโปรแกรม.cmd` แล้วเลือกไฟล์ในเครื่อง จะยังใช้ไฟล์ในโฟลเดอร์โปรแกรมเหมือนเดิม —
ป้ายบนแถบด้านบนบอกอยู่ว่าตอนนี้ใช้ชุดไหน)

### สองเครื่องแก้ไฟล์เดียวกันพร้อมกัน

โปรแกรมกันให้แล้ว — ตอนบันทึกจะส่ง eTag ของไฟล์ไปด้วย
ถ้ามีคนแก้ไฟล์นั้นไปก่อน OneDrive จะปฏิเสธการเขียน แล้วโปรแกรมจะถามว่า
จะโหลดไฟล์ล่าสุดมาแล้วเพิ่มรายการต่อท้ายให้ไหม **ของเดิมไม่หาย**

---

## แก้ปัญหา

| อาการ | สาเหตุ / วิธีแก้ |
|---|---|
| กดล็อกอินแล้วขึ้น `AADSTS9002326` หรือ CORS error | Redirect URI ลงทะเบียนผิดชนิด — ต้องเป็น **Single-page application (SPA)** ไม่ใช่ Web |
| ขึ้น `AADSTS50011: redirect URI does not match` | URI ใน Azure ไม่ตรงกับของจริง กด **☁ เปิดจาก OneDrive** → **คัดลอก Redirect URI** แล้วเอาไปใส่ให้ตรงเป๊ะ |
| ขึ้น `unauthorized_client` | ตอนสร้างแอปไม่ได้เลือก **Personal Microsoft accounts only** — สร้างใหม่ หรือแก้ที่ Manifest |
| หน้าต่างล็อกอินเปิดแล้วปิดเอง ไม่มีอะไรเกิดขึ้น | ป๊อปอัปถูกบล็อก — อนุญาตป๊อปอัปให้เว็บนี้ แล้วลองใหม่ (ถ้าบล็อกอยู่ โปรแกรมจะเปลี่ยนไปล็อกอินแบบเปลี่ยนหน้าให้เอง) |
| แก้โค้ดแล้วอัปขึ้น GitHub แต่แท็บเล็ตยังเห็นของเก่า | ปิดแอปแล้วเปิดใหม่ 1 รอบ (service worker เอาของใหม่จากเน็ตก่อนเสมอเมื่อออนไลน์) |
| อ่านสลิปไม่ได้ตอนไม่มีเน็ต | ยังไม่ได้กด **📥 เตรียมใช้ออฟไลน์** — ต่อ Wi-Fi แล้วกดหนึ่งครั้ง |
